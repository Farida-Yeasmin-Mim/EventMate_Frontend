import React from 'react';

const FilterPage = (props) =>{
    const {state} = CartState();
  console.log(state);
    return (
        <div>
            
        </div>
    );
}

export default FilterPage;